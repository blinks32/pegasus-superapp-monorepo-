rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all users for all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}